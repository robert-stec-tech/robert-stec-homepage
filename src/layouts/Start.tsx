import MainImg from '../components/MainImg';
import HelloText from '../components/HelloText';

const Start = () => {
  return (
    <section className="layout justify-center gap-[10vh]">
      <MainImg />
      <HelloText />
    </section>
  );
};

export default Start;
